import { useState } from "react";
import { View, SafeAreaView, ScrollView } from "react-native";
import { View } from "react-native";

import { Colors, icons, images, SIZES} from "react-native/Libraries/NewAppScreen";
const Home = () => {
    return(
        <view>
            <Text>Home</Text>
        </view>
    )
}
export default Home;
